## 当前软件品牌化不足

- 鼠标右击显示的选项 "AI 差异分析" 没有 Icon
- 分析完成的弹窗没有 Icon 展示

## 软件名称更改

软件名称正式改为 ： **diff-check**

## 优化方案

1. 盘点品牌相关入口与文案位置，建立替换清单（右键菜单显示名、GUI 标题、通知标题/正文、日志提示、配置目录、AppId、文档）。
   - 验证：用文本搜索确认所有“AI 差异分析/AI.DiffAssistant”等旧名称均已列入清单，并标注目标名称为 “diff-check”。

2. 设计并落地品牌图标资源：生成 `diff-check.ico` 作为应用图标，同时提供同款 `diff-check.png` 用于通知图标。
   - 验证：确认资源文件存在于发布可复制目录，并能在系统图像查看器中正常打开。

3. 调整右键菜单显示名称为 “diff-check”，并绑定图标到右键菜单项。
   - 验证：完成注册后在资源管理器右键文件查看菜单名称与图标；确认旧名称菜单不再出现。

4. 更新 Toast 通知使用应用图标，并确保点击通知/按钮能正常打开文件与文件夹。
   - 验证：触发一次成功通知，确认图标显示正确且点击行为无报错。

5. 修改可执行文件名称（不修改程序集名）：GUI 输出改为 `diff-check.exe`，CLI 输出改为 `diff-check-cli.exe`，并同步更新任何引用该路径的位置。
   - 验证：发布后检查 `publish` 目录包含上述两个新命名文件；右键菜单与 GUI 启动路径均能正确找到新文件。

6. 更新注册表键名并提供升级路径：旧键名迁移到新键名，确保升级后不残留旧项。
   - 验证：安装/注册后检查注册表仅存在新键名，且升级用户无需手动清理即可正常使用。

7. 更新日志路径、配置路径、AppId：默认配置路径改为 `%APPDATA%\\diff-check\\config.json`，日志路径改为 `%TEMP%\\diff-check.log`，Toast AppId 改为 `diff-check`，并提供从旧路径自动迁移。
   - 验证：首次启动时能从旧配置目录自动迁移到新目录，日志写入到新路径且通知 AppId 生效。

8. 更新 GUI 标题与可见品牌文案为 “diff-check”。
   - 验证：启动 GUI，确认窗口标题与界面文案为新名称。

9. 同步更新项目文档（PRD、架构、问题追踪、发布说明等）。
   - 验证：全文搜索不再出现旧名称的用户可见文案，文档描述与实际行为一致。

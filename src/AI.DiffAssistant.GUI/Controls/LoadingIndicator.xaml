<UserControl x:Class="AI.DiffAssistant.GUI.Controls.LoadingIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="32" d:DesignWidth="32">
    <UserControl.Resources>
        <SolidColorBrush x:Key="LoadingAccent" Color="{DynamicResource AccentColor}"/>
    </UserControl.Resources>

    <Grid x:Name="MainGrid" Width="24" Height="24">
        <Grid.RenderTransform>
            <RotateTransform x:Name="RotateTransform" Angle="0" CenterX="12" CenterY="12"/>
        </Grid.RenderTransform>

        <!-- 外圈轨迹 -->
        <Path x:Name="RingPath1"
              Data="M12,2 A10,10 0 0,1 22,12 A10,10 0 0,1 12,22 A10,10 0 0,1 2,12 A10,10 0 0,1 12,2"
              Stroke="{DynamicResource LoadingRingTrackBrush}"
              StrokeThickness="3"
              Fill="Transparent"
              Opacity="0.3"/>

        <!-- 内圈动画 -->
        <Path x:Name="RingPath2"
              Data="M12,2 A10,10 0 0,1 22,12 A10,10 0 0,1 12,22 A10,10 0 0,1 2,12 A10,10 0 0,1 12,2"
              Stroke="{DynamicResource LoadingRingBrush}"
              StrokeThickness="3"
              Fill="Transparent"
              StrokeDashArray="1, 3">
            <Path.OpacityMask>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Offset="0" Color="Transparent"/>
                    <GradientStop Offset="0.5" Color="White"/>
                    <GradientStop Offset="1" Color="White"/>
                </LinearGradientBrush>
            </Path.OpacityMask>
        </Path>
    </Grid>
</UserControl>
